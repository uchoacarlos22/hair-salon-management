    import { Box, Typography } from '@mui/material';

    const ManagerDashboard = () => {
      return (
        <Box sx={{ padding: 2 }}>
          <Typography variant="h4">Painel do Gestor</Typography>
          {/* Conteúdo do painel do gestor aqui */}
        </Box>
      );
    };

    export default ManagerDashboard;
